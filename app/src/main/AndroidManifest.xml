<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ggf.zpasnew">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zpasnew">
        <activity android:name=".PilihTk"></activity>
        <activity android:name=".DetailKegiatanTk" />
        <activity android:name=".halamantk" />
        <activity android:name=".Summary" />
        <activity android:name=".SpkWelcome" />
        <activity android:name=".DetailSpkInput" />
        <activity
            android:name=".DetailSPK"
            android:launchMode="singleTask" />
        <activity android:name=".MainActivity" />
        <activity android:name=".InputSPK" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>